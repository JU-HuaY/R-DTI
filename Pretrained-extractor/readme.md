Because SPDNet relies on matrix SVD operations, pre-trained models often require alternating CPU and GPU training, resulting in relatively long training times.
(Nearly 360h with an Intel Core i7-11700K CPU and two NVIDIA GeForce RTX 3090 cards). 
Our upcoming work aims to address and significantly improve this problem!
