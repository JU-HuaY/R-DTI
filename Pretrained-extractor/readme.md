Because SPDNet relies on too many matrix SVD operations for protein representation, pre-trained models often require alternating CPU and GPU training, resulting in relatively long training times.
(Nearly 360h with an Intel Core i7-11700K CPU and two NVIDIA GeForce RTX 3090 cards). 
Our upcoming work (MFSF) aims to address this problem effectively!
